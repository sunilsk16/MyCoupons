import { ShowForDirective } from './show-for.directive';

describe('ShowForDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowForDirective();
    expect(directive).toBeTruthy();
  });
});
